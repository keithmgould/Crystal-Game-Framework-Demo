define(['app/constants'], function (Constants) {

  var mediator = new Mediator(),
      world,
      allEntities = [],
      otherEntities = [],
      selfShip,
      loopCallbacks = [];

  var update = function () {
  
  }

  return {
   yin: "yang" + Constants.physics.width
  };
});
